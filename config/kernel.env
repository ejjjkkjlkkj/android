# Official Android Common Kernel source for the AccessibleAndroid VM profile.
# The monthly manifest branch is synchronized and then frozen with repo manifest -r.

KERNEL_MANIFEST_URL=https://android.googlesource.com/kernel/manifest
KERNEL_MANIFEST_BRANCH=common-android17-6.18-2026-09
KERNEL_FAMILY=android17-6.18
KERNEL_ARCH=x86_64
# The upstream server target uses linux_distro.fragment. The build pipeline
# injects kernel/vmware_x86_64.fragment there before Kleaf is invoked.
KERNEL_BUILD_TARGET=//common-modules/virtual-device:server_x86_64
KERNEL_DIST_TARGET=//common-modules/virtual-device:server_x86_64_dist
KERNEL_IMAGE_NAME=bzImage
KERNEL_REQUIRED_VM_MODULES=virtio-gpu.ko,virtio-rng.ko,virtio_input.ko,virtio_net.ko,virtio_snd.ko
